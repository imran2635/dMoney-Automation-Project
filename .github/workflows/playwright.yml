name: Playwright Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    timeout-minutes: 15
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Install Xvfb
        run: sudo apt-get update && sudo apt-get install -y xvfb

      - name: Run Playwright tests
        env:
          BASE_URL: ${{ secrets.BASE_URL }}
          ADMIN_EMAIL: ${{ secrets.ADMIN_EMAIL }}
          ADMIN_PASSWORD: ${{ secrets.ADMIN_PASSWORD }}
          SYSTEM_EMAIL: ${{ secrets.SYSTEM_EMAIL }}
          SYSTEM_PASSWORD: ${{ secrets.SYSTEM_PASSWORD }}
          GMAIL_BASE_EMAIL: ${{ secrets.GMAIL_BASE_EMAIL }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          EXISTING_CUSTOMER_PHONE: ${{ secrets.EXISTING_CUSTOMER_PHONE }}
        # Run in a virtual display instead of headless mode, since headless
        # Chromium's fingerprint differs from a real browser window.
        run: xvfb-run --auto-servernum npx playwright test --headed

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 14